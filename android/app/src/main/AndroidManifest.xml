<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="eu.thefit.celia">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA"/>

    <!-- Only the calorie scanner uses the camera, and it already tells the user
         when a device has none. The camera plugin declares this feature as
         required, which made Google Play hide the app from every camera-less
         device: 426 models that the previous release supported. -->
    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="false"
        tools:replace="android:required" />

    <!-- Celia shows no ads and has no analytics SDK, and the Play Console
         declaration says so. Google Play services libraries pull this permission
         in through manifest merge on their own, though, and a build that carries
         it while the declaration says otherwise is blocked from release. Removing
         it explicitly keeps the two in agreement whatever the dependencies do. -->
    <uses-permission
        android:name="com.google.android.gms.permission.AD_ID"
        tools:node="remove" />

    <!-- Requesting CAMERA makes Google Play treat a rear camera and autofocus as
         hardware the app cannot run without, which hides it from devices that
         have neither. Only the calorie scanner needs a camera and it already
         handles a device that has none. -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />

    <!-- Push-to-talk chat (speech_to_text) needs the mic. Camera stills still
         use enableAudio: false; this permission is for voice chat only. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- RECORD_AUDIO otherwise makes Play treat a mic as required hardware, the
         same filtering that dropped devices for the camera and fingerprint. -->
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" />

    <!-- androidx.biometric arrives through Firebase Auth and asks for these, but
         nothing in Celia authenticates with a fingerprint: sign-in is email and
         password. USE_FINGERPRINT in particular makes Play treat a fingerprint
         sensor as required hardware, which is what dropped support for devices
         the previous release reached. -->
    <uses-permission
        android:name="android.permission.USE_BIOMETRIC"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.USE_FINGERPRINT"
        tools:node="remove" />
    <uses-feature
        android:name="android.hardware.fingerprint"
        android:required="false" />
    <!-- Explicitly support phones and tablets; no orientation lock. -->
    <supports-screens
        android:smallScreens="true"
        android:normalScreens="true"
        android:largeScreens="true"
        android:xlargeScreens="true"
        android:anyDensity="true"
        android:resizeable="true" />

    <application
        android:label="Celia"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:resizeableActivity="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:screenOrientation="fullUser"
            android:resizeableActivity="true">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Android 11+: allow discovering the system TTS engine -->
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
        <!-- Android 11+: without this, speech_to_text cannot see the system
             recognizer and initialize() fails. -->
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>
</manifest>
